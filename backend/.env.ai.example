# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys

# Required: Your OpenAI API key
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Model to use for interpretations
# Default: gpt-4-turbo-preview
# Options: gpt-4-turbo-preview, gpt-4, gpt-3.5-turbo
OPENAI_MODEL=gpt-4-turbo-preview

# Optional: Maximum tokens for AI responses
# Default: 2000
# Range: 100-4000 (depending on model)
OPENAI_MAX_TOKENS=2000

# Optional: Temperature for response randomness
# Default: 0.7
# Range: 0.0-2.0 (lower = more focused, higher = more creative)
OPENAI_TEMPERATURE=0.7

# Cost Management Notes:
# - GPT-4 Turbo: ~$0.01 per 1K input tokens, ~$0.03 per 1K output tokens
# - GPT-3.5 Turbo: ~$0.0005 per 1K input tokens, ~$0.0015 per 1K output tokens
# - Typical natal chart interpretation: ~500 input tokens, ~1000 output tokens
# - Estimated cost per natal chart with GPT-4 Turbo: ~$0.04
# - Estimated cost per natal chart with GPT-3.5 Turbo: ~$0.002
