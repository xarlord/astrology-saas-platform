# PWA Environment Variables Template
# Copy this file to .env and fill in your actual values

# ========================================
# VAPID Keys for Web Push Notifications
# ========================================
# Generate VAPID keys using: npx web-push generate-vapid-keys
# These keys are required for push notification functionality

VAPID_PUBLIC_KEY=your_public_key_here
VAPID_PRIVATE_KEY=your_private_key_here
VAPID_SUBJECT=mailto:contact@astrology-saas.com

# ========================================
# Firebase Cloud Messaging (Optional)
# ========================================
# Required only for mobile push notifications via Firebase
# Leave empty if not using FCM

FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYour_Private_Key_Here\n-----END PRIVATE KEY-----\n
FIREBASE_CLIENT_EMAIL=your-client-email@your-project-id.iam.gserviceaccount.com

# ========================================
# Push Notification Configuration
# ========================================

# Time to live for push notifications (in seconds)
# Default: 24 hours (86400 seconds)
PUSH_TTL=86400

# Urgency level for push notifications: very-low | low | normal | high
PUSH_URGENCY=normal

# ========================================
# Notes
# ========================================
#
# 1. VAPID Keys Generation:
#    - Run: npx web-push generate-vapid-keys
#    - Add public key to frontend environment
#    - Keep private key secure and never commit to git
#
# 2. Firebase Setup (Optional):
#    - Create project at https://console.firebase.google.com
#    - Generate service account key
#    - Add credentials above
#    - Required for iOS/Android push notifications
#
# 3. Testing:
#    - Use the /api/v1/notifications/test endpoint
#    - Requires active subscription from frontend
#
# 4. Security:
#    - Never commit .env file to version control
#    - Use different keys for development and production
#    - Rotate keys periodically (recommended: every 90 days)
