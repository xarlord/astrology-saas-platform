---
description: Run the full backend test suite with coverage
---

# Run Backend Tests

1. Run: `cd backend && npx jest --coverage --verbose`
2. If any tests fail, analyze the failures and provide:
   - Which test(s) failed and why
   - The relevant source code causing the failure
   - A suggested fix
3. Report the coverage summary at the end
