---
description: Run frontend tests with Vitest
argument-hint: "[file-pattern]"
---

# Run Frontend Tests

1. Run: `cd frontend && npx vitest run $ARGUMENTS`
2. If a specific file pattern is provided, run: `cd frontend && npx vitest run --testPathPattern="$ARGUMENTS"`
3. Analyze any failures and suggest fixes
